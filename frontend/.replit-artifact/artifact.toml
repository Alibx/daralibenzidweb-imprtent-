kind = "web"
previewPath = "/"
title = "دار علي بن زيد للطباعة والنشر"
version = "1.0.0"
id = "artifacts/dar-ali"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 22433

[services.development]
run = "pnpm --filter @workspace/dar-ali run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/dar-ali", "run", "build" ]
publicDir = "artifacts/dar-ali/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "22433"
BASE_PATH = "/"
